<h2>User info</h2>
<% @book.each do |book| %>
<%= image_tag no_image, size: "200x200" %>
<% end %>

<h2>New book</h2>
<%= form_with model: Book.new do |f| %>
  <h4>Title</h4>
  <%= f.text_field :title %>
  
  <h4>Opinion</h4>
  <%= f.text_area :body %>
  
  <%= f.submit 'Create Book' %>
<% end %>